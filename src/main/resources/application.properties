spring.application.name=dissertation-management-system
# Local secrets (the database password) live in application-local.properties at the
# project root. That file is gitignored, so no credential enters git history.
# "optional:" means the app still starts if it is absent -- it fails at the datasource
# with a clear message instead. See application-local.properties.example.
spring.config.import=optional:file:./application-local.properties

spring.datasource.url=jdbc:postgresql://localhost:5432/dms
spring.datasource.username=postgres
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.servlet.multipart.max-file-size=25MB
spring.servlet.multipart.max-request-size=25MB
spring.thymeleaf.cache=false
logging.level.com.dms=DEBUG
logging.level.org.springframework.security=INFO
